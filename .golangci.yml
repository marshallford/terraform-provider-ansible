version: "2"
linters:
  default: all
  disable:
  - depguard
  - exhaustruct
  - funlen
  - godox
  - lll
  - perfsprint
  - recvcheck
  - revive
  - wsl
  settings:
    varnamelen:
      ignore-type-assert-ok: true
      ignore-map-index-ok: true
      ignore-chan-recv-ok: true
